@* Unused — this only ever held the "Contact" button that opened the old
   contact modal. Contact.razor is now rendered directly on the home page,
   so there's nothing left for this component to do. *@
